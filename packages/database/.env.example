# Copia a `packages/database/.env` o exporta antes de migrar / seed.
# Debe coincidir con el Postgres que levantas (p. ej. `pnpm db:up` en la raíz).

DATABASE_URL="postgresql://pfo:pfo@127.0.0.1:5433/pfo"
