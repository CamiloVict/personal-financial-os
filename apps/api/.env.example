# Copia este archivo a `apps/api/.env` (o exporta las variables en tu terminal).

# PostgreSQL — misma URL que en `packages/database/.env` (ver `docker-compose.yml` en la raíz).
DATABASE_URL="postgresql://pfo:pfo@127.0.0.1:5433/pfo"
